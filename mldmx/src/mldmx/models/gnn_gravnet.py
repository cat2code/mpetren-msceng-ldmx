import torch.nn as nn
from torch_geometric.nn import GravNetConv

# This script should hold two models for the GNN-based ECal hit 
# classification tasks and the two models should use the GravNetConv
# layer from torch_geometric.nn, which is a  graph convolutional 
# layer that can learn to adapt the graph structure during training. 


# Implement a per-node classifier for ECal hits with TriggerPadTracks context nodes here


# Implement a per-node classifier for (only) ECal hits as nodes here

