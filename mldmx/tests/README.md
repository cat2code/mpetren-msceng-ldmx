# ml_ldmx/tests

This directory should contains scripts that test some feature that should be maintained in new implementations. The testing script should test the feature, for example sharding ROOT files, and then be done with it.

One could view it as that this directory should contain mostly so called smoke tests. 